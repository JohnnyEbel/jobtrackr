services:
  api:
    build: ./app
    container_name: jobtrackr-api
    ports:
      - "5000:5000"
    volumes:
      - jobtrackr_data:/app/data
    environment:
      - DB_PATH=/app/data/jobs.db
    restart: unless-stopped

volumes:
  jobtrackr_data: